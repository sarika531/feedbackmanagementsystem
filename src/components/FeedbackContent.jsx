// components/FeedbackContent.js

const FeedbackContent = () => {
  return (
    <div style={{ padding: '20px', color: '#fff' }}>
      <h1>Feedback Page</h1>
      <p>This is the feedback content displayed under the navbar.</p>
    </div>
  );
};

export default FeedbackContent;
